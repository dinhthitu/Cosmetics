spring.application.name=Cosmetics
spring.datasource.url= ${DATABASE-URL}
spring.datasource.username= ${DATABASE-USERNAME}
spring.datasource.password= ${DATABASE-PASSWORD}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.default_schema=cosmetic_schema
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
logging.level.org.springframework.web.filter.CommonsRequestLoggingFilter=DEBUG
paypal.mode = sandbox
paypal.client.id = ${PAYPAL-CLIENT-ID}
paypal.client.secret = ${PAYPAL-CLIENT-SECRET}
jwt_secret = ${JWT-SECRET}

spring.mail.host = smtp.gmail.con
spring.mail.port = 587
spring.mail.username = dinhthitu3773@gmail.com
spring.mail.password = zbre zelm ipsw dlaj
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.cloudinary.cloud.name = ${CLOUD-NAME}
spring.cloudinary.api.key = ${CLOUD-API-KEY}
spring.cloudinary.api.secret = ${CLOUD-API-SECRET}

